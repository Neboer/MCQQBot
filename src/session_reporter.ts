// 用来报告用户登录登出，上线下线的消息事务。
// 用户上线报告、下线报告、连接报告
import Bot from "./bot/bot";
import logger from "./bot/logging";
import MCServer from "./lighting_mc/minecraft_server";

function bind_session_reporter(bot: Bot, mc_server: MCServer) {

}